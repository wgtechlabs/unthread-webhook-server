NODE_ENV=development
PORT=3000
TARGET_PLATFORM=telegram
REDIS_URL=redis://localhost:6379
UNTHREAD_WEBHOOK_SECRET=your_signing_secret_here

# PostgreSQL Database Configuration
# Required: Full connection string
DATABASE_URL=postgresql://username:password@localhost:5432/unthread_webhook_db

# Optional: Individual connection parameters (used if DATABASE_URL fails)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=unthread_webhook_db
# DATABASE_USER=username
# DATABASE_PASSWORD=password